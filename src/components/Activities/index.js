import React from 'react';
import { ScrollContainer, Header, Title } from './styles';

export default function Activities() {
  return (
    <ScrollContainer>
      <Header>
        <Title>Activities</Title>
      </Header>
    </ScrollContainer>
  );
}